OpenGL renderer

The source files for the project are in server and solution. Server contains a very basic license checker and solution contains the actual renderer.

dll files in .lib/bin/ need to be moved to system32 if you want to run the program after compiling it.
